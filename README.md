# 福岡之旅 2025 - 行程網站 🌸

一個漂亮的旅遊行程網站，幫助你規劃和追蹤福岡之旅（2025年12月13-20日）。

## ✨ 功能特色

- 📅 **完整行程表** - 8天詳細行程時間軸
- 📋 **預約清單** - 所有需要預約的餐廳和活動
- 🔗 **直接連結** - 一鍵前往預約網站
- ✅ **進度追蹤** - 勾選已完成的預約（自動保存）
- 🎨 **漂亮設計** - 現代化日本風格UI
- 📱 **響應式** - 手機/平板/電腦完美顯示
- ⏰ **倒數計時** - 顯示距離出發還有幾天

## 🚀 快速開始

### 1. 安裝依賴

```bash
cd website
npm install
```

### 2. 本地運行

```bash
npm run dev
```

打開瀏覽器訪問 [http://localhost:3000](http://localhost:3000)

### 3. 建立生產版本

```bash
npm run build
npm start
```

## 📦 部署到 Vercel

### 方法 1: 通過 Vercel 網站部署（推薦）

1. 將此專案推送到 GitHub:
   ```bash
   git add .
   git commit -m "Add Fukuoka 2025 itinerary website"
   git push
   ```

2. 前往 [Vercel](https://vercel.com) 並登入

3. 點擊 "New Project"

4. 導入你的 GitHub repository

5. 配置設定：
   - **Framework Preset**: Next.js
   - **Root Directory**: `website`
   - **Build Command**: `npm run build`
   - **Output Directory**: 留空（使用默認）

6. 點擊 "Deploy"

7. 完成！你會獲得一個 `https://your-project.vercel.app` 網址

### 方法 2: 通過 Vercel CLI 部署

```bash
# 安裝 Vercel CLI
npm install -g vercel

# 登入
vercel login

# 部署
cd website
vercel
```

### 方法 3: 自動部署（推薦）

Vercel 會自動檢測你的 GitHub repository 變更並重新部署：

1. 完成上面的方法 1
2. 之後每次 push 到 main/master 分支，Vercel 會自動重新部署
3. 每個 Pull Request 也會獲得預覽部署

## 📂 專案結構

```
website/
├── app/
│   ├── layout.tsx          # 主布局（導航欄、頁腳）
│   ├── page.tsx            # 首頁（行程表）
│   ├── reservations/
│   │   └── page.tsx        # 預約清單頁面
│   └── globals.css         # 全局樣式
├── data/
│   ├── itinerary.json      # 行程資料
│   └── reservations.json   # 預約資料
├── public/                 # 靜態資源
├── package.json           # 依賴配置
├── next.config.js         # Next.js 配置
├── tailwind.config.ts     # Tailwind CSS 配置
└── tsconfig.json          # TypeScript 配置
```

## 🎨 技術棧

- **Framework**: [Next.js 14](https://nextjs.org/) (App Router)
- **語言**: TypeScript
- **樣式**: Tailwind CSS
- **部署**: Vercel
- **UI**: 響應式設計，支持深色模式

## 🔧 自定義

### 修改行程資料

編輯 `data/itinerary.json` 來更新每日行程。

### 修改預約清單

編輯 `data/reservations.json` 來更新預約項目和連結。

### 修改顏色主題

編輯 `tailwind.config.ts` 中的 `fukuoka` 顏色配置：

```typescript
fukuoka: {
  pink: '#FFB7C5',
  blue: '#2C3E50',
  gold: '#D4AF37',
  cream: '#FFF8F0',
}
```

## 📱 功能說明

### 行程表頁面
- 顯示完整 8 天行程
- 每天的活動、時間、地點
- 需預約的項目特別標記
- 倒數計時功能

### 預約清單頁面
- 所有需要預約的項目列表
- 優先級標示（⭐ 1-5 星）
- 預約方法和連結
- 備選方案（Backup Plans）
- 可勾選進度追蹤
- 進度條顯示完成度

## 🌟 特色功能

### 進度追蹤
你的預約進度會自動保存在瀏覽器的 localStorage 中，即使關閉瀏覽器也不會丟失。

### 響應式設計
網站在所有設備上都完美顯示：
- 📱 手機（旅行時查看）
- 📊 平板
- 💻 電腦

### 直接連結
所有預約網站都有直接連結，點擊即可前往：
- TableCheck
- Klook
- Viator
- Omakase.in
- RentalCars
- Instagram

## 🐛 問題排查

### 本地開發遇到問題

```bash
# 清除緩存並重新安裝
rm -rf node_modules .next
npm install
npm run dev
```

### Vercel 部署失敗

1. 確認 Root Directory 設定為 `website`
2. 確認所有檔案都已推送到 GitHub
3. 查看 Vercel 的部署日誌

## 📝 更新內容

要更新網站內容：

1. 修改 `data/itinerary.json` 或 `data/reservations.json`
2. 提交並推送到 GitHub:
   ```bash
   git add .
   git commit -m "Update itinerary"
   git push
   ```
3. Vercel 會自動重新部署（約 1-2 分鐘）

## 🎉 享受你的福岡之旅！

有任何問題或建議，歡迎修改和優化這個網站！

---

**Made with ❤️ for Fukuoka Trip 2025 🌸**

